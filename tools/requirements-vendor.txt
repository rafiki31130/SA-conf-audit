# Vendored dependency under `bin/lib/` - exactly one, pinned down to the patch level.
#
# The `confaudit` core has NO third-party dependency: the REST calls are written as raw
# HTTP on top of `urllib` + `ssl` from the standard library, and the tests rely on
# `unittest`. This single line is therefore the whole license, audit and CVE surface
# the app carries.
#
# Any additional dependency is a trade-off that must be escalated: in a public
# repository, every vendored package is a surface the app carries with no update
# mechanism.
#
# Version 3.x of the SDK requires Python >= 3.13 and is therefore not installable on
# the interpreter shipped with Splunk Enterprise 9.x. The 2.1.1 series is the last
# compatible one.
#
# Upstream license: Apache-2.0 (the same as this app's).

splunk-sdk==2.1.1 --hash=sha256:46300d52f09e0aed7e5962ce2ba08ef54421ffb3a538c6af6164dcbf9f075faa
